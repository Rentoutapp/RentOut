package org.example.project.ui.components

/**
 * Returns the current hour of the day (0–23) in the device's local timezone.
 * Implemented via expect/actual per platform.
 */
expect fun getCurrentHour(): Int
